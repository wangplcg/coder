<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd">

    <bean id="clinicDataSource" name="dataSource_clinic" class="com.alibaba.druid.pool.DruidDataSource"
          init-method="init" destroy-method="close">
        <property name="url" value="${clinic.pool.url}" />
        <property name="username" value="${clinic.pool.username}"/>
        <property name="password" value="${clinic.pool.password}"/>
        <property name="driverClassName" value="${clinic.pool.driver}"/>
        <property name="filters" value="stat" />
        <property name="initialSize" value="${clinic.pool.initialSize}"/>
        <property name="minIdle" value="${clinic.pool.minIdle}" />
        <property name="maxActive" value="${clinic.pool.maxActive}" />
        <property name="maxWait" value="60000" />
        <property name="timeBetweenEvictionRunsMillis" value="3000" />
        <property name="minEvictableIdleTimeMillis" value="300000" />
        <property name="validationQuery" value="SELECT 1 from dual" />
        <property name="testWhileIdle" value="true" />
        <property name="testOnBorrow" value="false" />
        <property name="testOnReturn" value="false" />
        <property name="poolPreparedStatements" value="true" />
        <property name="maxPoolPreparedStatementPerConnectionSize" value="20" />
        <property name="removeAbandoned" value="true" />
        <property name="removeAbandonedTimeout" value="1800" />
        <property name="logAbandoned" value="true" />
    </bean>

    <!-- 事务管理 -->
    <bean id="clinicTransactionManager"
          class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
        <property name="dataSource" ref="clinicDataSource" />
        <qualifier value="clinicTransactionManager"/>
    </bean>

    <bean id="clinicSqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
        <property name="dataSource" ref="clinicDataSource" />
        <!-- 自动扫描mapping.xml文件 -->
        <property name="mapperLocations">
            <array>
                <value>classpath*:mybatis/mapper/clinic/*.xml</value>
            </array>
        </property>
        <property name="configLocation" value="classpath:mybatis/clinic_sqlMapConfig.xml"/>
    </bean>

    <!-- 接口所在包名，Spring会自动查找其下的类 -->
    <bean class="org.mybatis.spring.mapper.MapperScannerConfigurer">
        <property name="basePackage" value="cn.com.sun.mapper.clinic" />
        <property name="sqlSessionFactoryBeanName" value="clinicSqlSessionFactory"/>
    </bean>
</beans>